import React from 'react'

import { 
  HeaderWrapper, 
  HeaderContainer,
  HeaderTitle
} from './styled'

const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderContainer>
        <HeaderTitle>
          CUPONERIA DAILY
        </HeaderTitle>
      </HeaderContainer>
    </HeaderWrapper>
  )
}

export default Header;